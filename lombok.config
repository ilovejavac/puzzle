# EqualsAndHashCode/ToString 默认调用父类
lombok.equalsAndHashCode.callSuper = call
lombok.toString.callSuper = call

# 链式调用
lombok.accessors.chain = true
lombok.accessors.fluent = false

# @NonNull 检查
lombok.nonNull.exceptionType = NullPointerException

# 日志
lombok.log.fieldName = log
lombok.log.fieldIsStatic = true

# Builder
lombok.builder.className = Builder
lombok.singular.useGuava = false

# 生成的代码加 @Generated 注解(用于代码覆盖率统计)
lombok.addLombokGeneratedAnnotation = true

# 构造函数生成 @ConstructorProperties (用于JSON反序列化)
lombok.anyConstructor.addConstructorProperties = true

# 字段默认 private final (配合 @Value)
lombok.fieldDefaults.defaultPrivate = true
lombok.fieldDefaults.defaultFinal = false

# 禁用某些注解(可选)
# lombok.data.flagUsage = error
# lombok.val.flagUsage = warning